<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="wildlife"
            type="com.scubadeving.sd_playground.data.model.wildlife.Wildlife" />

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/wildlife_card_container"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:elevation="@dimen/card_elevation"
        android:onClick="@{clickListener}"
        app:cardBackgroundColor="@android:color/holo_green_light"
        app:cardElevation="@dimen/card_elevation"
        app:cardPreventCornerOverlap="true"
        app:cardUseCompatPadding="true"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.SDPlayground.Card">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- TODO: app:imageFromUrl="@{wildlife.imageUrl}" -->
            <ImageView
                android:id="@+id/wildlife_card_image"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:contentDescription="@string/a11y_wildlife_image"
                android:scaleType="centerCrop"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:src="@drawable/ic_wildlife" />

            <TextView
                android:id="@+id/wildlife_card_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="start|bottom"
                android:background="#33000000"
                android:ellipsize="end"
                android:gravity="center_horizontal"
                android:padding="5dp"
                android:text="@{wildlife.commonName}"
                android:textAppearance="?attr/textAppearanceListItem"
                android:textColor="@color/white"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                tools:text="Manta Ray" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>